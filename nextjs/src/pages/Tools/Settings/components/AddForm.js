// Imports
import React from 'react';
import Form from './Form';

/**
 * AddForm
 * Middle form for the settings page
 * @returns {JSX.Element}
 */
const AddForm = () => {
  return (
    <Form/>
  );
};

export default AddForm;
