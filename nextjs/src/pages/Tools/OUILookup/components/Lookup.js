// Imports
import React from 'react';
import Form from './Form';

/**
 * Lookup
 * Middle form for looking up an OUI
 * @returns {JSX.Element}
 */
const Lookup = () => {
  return (
    <Form/>
  );
};

export default Lookup;
