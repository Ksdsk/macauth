// Imports

import React from 'react';
import Form from './Form';

/**
 * AddForm
 * Middle form for adding a new MAC address
 * @returns {JSX.Element}
 */
const AddForm = () => {
  return (
    <Form/>
  );
};

export default AddForm;
