// Imports
import React from 'react';
import AddressList from './MacAddress/AddressList';

/**
 * MainPage
 * Page for the main page (defaulted to AddressList)
 * @returns {JSX.Element}
 */
const MainPage = () => {
  return (
    <AddressList />
  );
};

export default MainPage;
