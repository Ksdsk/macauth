// Imports
import React from 'react';
import Authorizer from './MacAddress/Authorizer';

/**
 * AuthorizerPage
 * Page for the authorizer page
 * @returns {JSX.Element}
 */
const AuthorizerPage = () => {
  return (
    <Authorizer />
  );
};

export default AuthorizerPage;
